/node_modules
/.next
# Alternate build dirs (a `next build -d`/distDir run drops ~50MB here) — build output, never source.
/.next-*
/out
next-env.d.ts
*.tsbuildinfo
.vercel

# Local secrets — never commit. .env.example (no real values) is the tracked template.
.env
.env.local
.env*.local
